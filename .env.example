# Murmura — Environment Variables
# For advanced usage, see: .env.advanced.example

# ── REQUIRED (Minimum to run) ────────────────────────────────────────────────
# To run without API keys, set DEMO_MODE=true
DEMO_MODE=false

# Preferred provider: OpenRouter (Unified access to DeepSeek, Claude, Llama 3)
OPENROUTER_API_KEY=sk-or-your-openrouter-key

# Security: Generate with `openssl rand -hex 32`
AUTH_SECRET_KEY=your-secret-key-here

# Encrypt API keys and connector secrets at rest.
# Generate with: python -c "import secrets, base64; print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"
DATA_ENCRYPTION_KEY=your-32-byte-base64url-key

# ── ADVANCED (Optional) ──────────────────────────────────────────────────────
DEBUG=false
# Current DB filename is murmura.db; legacy murmuroscope.db is auto-migrated when found.
DATABASE_PATH=data/murmura.db
LOG_LEVEL=INFO

# See .env.advanced.example for:
# - Google / OpenAI / Anthropic direct integration
# - External Macro Feed (FRED / World Bank)
# - Simulation Concurrency Limits
# - Custom Frontend URL
